<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Hibana</title>
    <style>
      :root {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        color: #0f172a;
        background-color: #f8fafc;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      header,
      footer {
        background: #111827;
        color: #f9fafb;
        padding: 1rem;
      }

      main {
        flex: 1;
        padding: 2rem;
        max-width: 720px;
        width: min(100%, 720px);
        margin: 0 auto;
      }

      a {
        color: #2563eb;
      }

      ul {
        padding-left: 1.5rem;
      }
    </style>
  </head>
  <body>
    <header>
      <strong>Hibana</strong>
    </header>

    <main>
      <%= yield %>
    </main>

    <footer>
      Cloudflare Workers + Ruby powered by Hibana
    </footer>
  </body>
</html>
